# Versions verified together on Windows with Python 3.11.
# Keep the complete runtime/build chain pinned so a release cannot silently
# change because a transitive dependency published a new version.
pywebview==6.2.1
pythonnet==3.1.0
clr_loader==0.3.1
cffi==2.0.0
pycparser==3.0
bottle==0.13.4
proxy_tools==0.1.0
typing_extensions==4.15.0

PyInstaller==6.20.0
altgraph==0.17.5
packaging==26.0
pefile==2024.8.26
pyinstaller-hooks-contrib==2026.5
pywin32-ctypes==0.2.3
setuptools==65.5.0

Pillow==12.2.0
python-docx==1.2.0
lxml==6.1.1
